# 论坛系统交互与 UI 规范（MVP）

## 1. 文档信息

- 文档版本：`v1.0-draft`
- 适用范围：论坛 MVP（手机端 H5 + PC Web）
- 关联文档：
  - `PRD.md`
  - `ARCHITECTURE.md`
  - `OPENAPI_CONTRACT.md`

---

## 2. 目标与原则

## 2.1 文档目标

1. 统一移动端与 PC 端的交互行为和视觉结构。
2. 降低前端实现歧义，减少联调期反复改动。
3. 为后续设计稿（Figma）和组件库建设提供规则基线。

## 2.2 设计原则

1. **Mobile First**：优先保证 375 宽度核心流程可用。
2. **一致性**：同类组件在不同页面行为一致。
3. **可反馈**：每次用户操作都必须有明确反馈。
4. **可恢复**：危险操作需要确认并支持撤销/提示。
5. **可访问**：键盘可操作、颜色对比合理、语义化标签完整。

---

## 3. 设备与断点规范

## 3.1 断点定义

- `xs`：`< 576`（小屏手机）
- `sm`：`576 ~ 767`（大屏手机）
- `md`：`768 ~ 1023`（平板）
- `lg`：`1024 ~ 1439`（PC）
- `xl`：`>= 1440`（大屏 PC）

## 3.2 页面宽度与栅格

1. 手机端：单列布局，左右边距 `16px`。
2. 平板：单列增强布局，左右边距 `24px`。
3. PC：
   - 内容容器最大宽度 `1200px`。
   - 推荐双栏：主内容 `1fr`，侧栏 `320px`。
4. 栅格间距（Gutter）统一 `16px`，PC 可放大至 `24px`。

## 3.3 触控与点击区域

1. 所有可点击主操作区域高度 >= `44px`。
2. 图标按钮可点击区域 >= `40px * 40px`。
3. 相邻可点击元素间距 >= `8px`，避免误触。

---

## 4. 信息架构与导航规则

## 4.1 一级信息架构

1. 首页（板块聚合）
2. 板块页（帖子列表）
3. 帖子详情页（内容 + 评论）
4. 发帖页
5. 登录/注册页
6. 个人中心（我的帖子/我的收藏/通知）
7. 管理后台（版主、管理员）

## 4.2 导航模式

1. 手机端：
   - 顶部导航 + 底部主导航（首页、发帖、通知、我的）。
2. PC 端：
   - 顶部主导航 + 页面侧栏导航（按页面需要）。
3. 导航状态：
   - 当前页高亮。
   - 未登录点击受限入口时，弹登录引导。

## 4.3 回退规则

1. 详情页返回优先回到来源列表并保持滚动位置。
2. 来源不可用时回到默认列表页（按最近板块）。
3. 发帖完成后跳详情页，并保留“返回列表”入口。

---

## 5. 全局交互状态规范

## 5.1 页面级状态

每个页面必须覆盖：

1. `Loading`：骨架屏或局部加载占位。
2. `Empty`：空数据说明 + 引导动作（如“去发第一条帖子”）。
3. `Error`：错误文案 + 重试按钮。
4. `Offline/Timeout`：网络异常提示（Toast + 页面兜底）。

## 5.2 组件级状态

交互组件（按钮、输入、列表项）至少支持：

1. 默认（Default）
2. 悬停（Hover，PC）
3. 按下（Active）
4. 聚焦（Focus，可键盘访问）
5. 禁用（Disabled）
6. 加载（Loading）
7. 错误（Error，表单组件）

## 5.3 反馈层级

1. **Toast**：轻量成功/失败提示，自动消失（2~3 秒）。
2. **Inline Message**：表单字段就近错误信息。
3. **Modal**：高风险或不可逆操作确认。
4. **Page Notice**：系统级问题（维护、权限变化）。

---

## 6. 页面级 UI 与交互规范

## 6.1 首页（板块聚合）

### 布局

- 顶部：搜索框 + 登录入口/用户头像
- 中部：板块卡片列表
- 底部（移动端）：主导航

### 交互

1. 点击板块卡片进入板块页。
2. 未登录用户可浏览内容。
3. 搜索输入防抖 `300ms`，回车触发搜索页。

## 6.2 板块页（帖子列表）

### 列表项内容

- 标题、摘要、作者、发布时间、互动计数（赞/评）

### 交互

1. 点击列表项进入帖子详情。
2. 支持排序切换（最新/最热）。
3. 支持下拉刷新（移动端）与分页加载（PC）。
4. 空态展示“暂无帖子”并提供“去发帖”入口。

## 6.3 帖子详情页

### 模块

1. 帖子主体（标题、作者、正文、附件）
2. 互动栏（点赞、收藏、举报）
3. 评论区（评论列表 + 评论输入）

### 交互

1. 点赞/收藏立即乐观更新，失败时回滚并提示。
2. 举报入口始终可见，提交后提示“已收到处理”。
3. 评论提交成功后自动插入新评论并清空输入框。
4. 删除评论后按产品配置展示占位文案或移除节点。

## 6.4 发帖页

### 输入项

1. 板块选择（必填）
2. 标题（5~100）
3. 正文（富文本或 Markdown）
4. 附件上传（可选，走 MinIO 直传）

### 交互

1. 表单未改动时离开页面不提示。
2. 表单有改动未提交时，离开页面弹确认框。
3. 点击发布按钮进入 Loading，防重复提交（幂等键）。
4. 发布成功跳转详情页，失败保留输入内容。

## 6.5 登录/注册页

### 交互

1. 输入合法性前端即时校验（邮箱格式、密码强度）。
2. 登录失败提示统一文案，不暴露账号存在性。
3. 封禁账号提示单独错误态（含联系客服入口可扩展）。

## 6.6 个人中心

### 标签页

1. 我的帖子
2. 我的收藏
3. 我的通知
4. 账号设置

### 交互

1. 通知支持“全部已读”。
2. 列表支持分页与空态。
3. 昵称修改触发唯一性校验失败时就地提示。

## 6.7 管理后台（版主/管理员）

### 页面

1. 举报列表
2. 举报详情
3. 审核处理面板
4. 用户状态管理

### 交互

1. 审核动作（忽略/下架/删除/封禁）必须二次确认。
2. 提交审核后刷新状态，并展示操作成功反馈。
3. 失败时保留处理备注，方便修改后重试。

---

## 7. 核心组件规范

## 7.1 Button

1. 类型：`Primary`、`Secondary`、`Danger`、`Text`。
2. 尺寸：`L(44)`、`M(40)`、`S(32)`。
3. Loading 状态：
   - 显示 spinner。
   - 文案可保持或切为“处理中”。
4. Disabled 状态：
   - 不可点击。
   - 禁用样式与正常态明显区分。

## 7.2 Input / Textarea

1. 标签、占位、帮助文案三者职责分离。
2. 错误信息贴近字段，红色提示 + icon（可选）。
3. 长度限制显示计数器（如 `32/100`）。
4. Textarea 支持最小高度与自动增长（可选）。

## 7.3 PostCard

1. 结构：标题 -> 摘要 -> 元信息 -> 互动计数。
2. 标题最多 2 行，超出省略。
3. 整卡可点，内部按钮（点赞/收藏）需事件隔离。

## 7.4 CommentItem

1. 显示作者、时间、正文、回复按钮。
2. 回复输入在当前评论下内联展开。
3. 删除状态支持“该评论已删除”占位样式。

## 7.5 Modal

1. 用于高风险操作确认与不可逆动作。
2. 包含标题、正文、取消、确认按钮。
3. Esc、蒙层点击关闭需按场景控制（危险操作默认不允许误关）。

## 7.6 Toast

1. 位置：顶部或右上角（PC），顶部（移动端）。
2. 类型：成功、失败、警告、信息。
3. 自动消失时长：
   - 成功：2 秒
   - 失败：3 秒

## 7.7 Pagination / Infinite Scroll

1. PC：推荐分页器 + 每页数量可配置（20/50）。
2. 移动端：推荐无限滚动，需提供“回到顶部”。
3. 到底状态明确提示“没有更多了”。

---

## 8. 表单与错误处理规范

## 8.1 校验时机

1. `onBlur`：格式校验（邮箱等）。
2. `onSubmit`：必填与业务规则校验。
3. 服务端返回错误后，映射到具体字段或全局提示。

## 8.2 错误文案规则

1. 文案可执行：告诉用户下一步怎么做。
2. 不暴露内部系统信息（堆栈、SQL、服务名）。
3. 同一错误在全站用词一致。

---

## 9. 可访问性（A11y）与易用性

1. 所有表单控件有可读 label。
2. 可交互元素支持键盘焦点与 Enter/Space 操作。
3. 焦点态可见，不可仅依赖颜色变化。
4. 文本与背景对比度满足 WCAG AA（建议）。
5. 图标按钮必须有 `aria-label`。
6. 图片需有替代文本（装饰图除外）。

---

## 10. 文案与语义规范

1. 操作文案动词开头：如“发布帖子”“删除评论”。
2. 危险动作使用明确结果描述：“删除后不可恢复”。
3. 时间展示：
   - 列表使用相对时间（如“3 分钟前”）
   - 详情使用绝对时间（如“2026-04-22 18:30”）

---

## 11. 设计 Token 建议（初版）

## 11.1 间距

- `space-1 = 4`
- `space-2 = 8`
- `space-3 = 12`
- `space-4 = 16`
- `space-5 = 20`
- `space-6 = 24`

## 11.2 圆角

- `radius-sm = 6`
- `radius-md = 10`
- `radius-lg = 14`

## 11.3 阴影

- `shadow-sm`：卡片轻阴影
- `shadow-md`：悬浮层
- `shadow-lg`：高优先弹层

## 11.4 层级（z-index）

- `dropdown = 1000`
- `sticky = 1100`
- `modal = 1300`
- `toast = 1400`

---

## 12. 页面与组件验收清单

## 12.1 页面验收

- [ ] 首页、板块页、详情页、发帖页、登录页在手机与 PC 均可完整操作
- [ ] 各页面均有 Loading/Empty/Error 状态
- [ ] 未登录用户点击受限操作会触发登录引导
- [ ] 关键流程（发帖、评论、举报）有明确成功/失败反馈

## 12.2 组件验收

- [ ] Button/Input/Modal/Toast/PostCard/CommentItem 状态完整
- [ ] 组件在移动端触控区域达标（>=44）
- [ ] 组件在 PC 支持 hover/focus 键盘访问

## 12.3 管理流程验收

- [ ] 举报处理动作具备二次确认
- [ ] 审核成功后列表状态实时更新
- [ ] 审核失败时保留输入并可重试

---

## 13. 待确认事项

1. 帖子正文编辑器最终采用富文本还是 Markdown。
2. 评论删除后是否长期保留占位文案。
3. 移动端是否强制启用底部导航 4 Tab。
4. 管理后台是否单独域名与独立样式体系。
5. 是否同步定义深色模式（本期默认不做）。

