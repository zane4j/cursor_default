# 论坛系统 PRD（产品需求文档）

## 1. 文档信息

- 产品名称：社区论坛系统（支持手机端与 PC 端）
- 文档版本：`v1.0`
- 状态：待评审
- 关联文档：
  - `ARCHITECTURE.md`
  - `PRE_CODING_DOCS.md`

---

## 2. 背景与目标

### 2.1 背景

需要建设一个从 0 到 1 的论坛产品，满足用户在移动端与 PC 端的内容发布、交流互动与社区治理需求。第一阶段以 MVP 快速上线验证用户活跃与留存，技术实现采用前后端分离。

### 2.2 业务目标（MVP）

1. 建立可用的内容社区闭环：浏览 -> 发帖 -> 评论 -> 通知回流。
2. 建立基础社区治理能力：举报、审核、封禁、删帖。
3. 支持手机端与 PC 端响应式访问，保证核心流程体验一致。

### 2.3 成功标准（上线后 30 天观察）

1. 日活用户（DAU）达到目标阈值（由业务侧另行填写）。
2. 发帖用户占活跃用户比例 >= 10%。
3. 帖子评论率 >= 20%（有评论帖子 / 总帖子）。
4. 举报处理及时率 >= 95%（24 小时内处理）。

---

## 3. 范围定义（Scope）

### 3.1 本期包含（In Scope）

1. 账号系统：注册、登录、退出、个人资料维护。
2. 板块系统：板块列表、板块内帖子列表。
3. 帖子系统：发布、编辑、删除、详情浏览、分页。
4. 评论系统：发表评论、回复评论、删除本人评论。
5. 互动系统：点赞、收藏。
6. 通知系统：被回复通知、被点赞通知。
7. 治理系统：举报、后台审核、下架内容、封禁用户。
8. 响应式适配：手机端（H5）与 PC 端同一前端代码实现。

### 3.2 本期不包含（Out of Scope）

1. 站内私信/即时聊天。
2. 推荐算法流与个性化 Feed。
3. 多租户与组织级隔离。
4. 小程序/原生 App（本期仅移动端浏览器）。
5. 复杂搜索引擎能力（先做基础搜索）。

---

## 4. 用户角色与权限

## 4.1 游客

- 可浏览公开板块与帖子详情。
- 不可发帖、评论、点赞、收藏、举报。

## 4.2 注册用户

- 可发帖、评论、点赞、收藏、举报。
- 可编辑/删除自己的帖子与评论（受时间与状态约束）。

## 4.3 版主

- 拥有指定板块内容审核权限。
- 可处理举报、下架违规内容。

## 4.4 管理员

- 全站审核、封禁用户、查看审计记录。

---

## 5. 核心用户旅程

### 5.1 新用户内容消费与互动闭环

1. 用户访问首页并浏览板块。
2. 进入板块查看帖子列表，打开帖子详情。
3. 登录后发表评论并点赞。
4. 帖子作者收到通知并回帖，形成二次互动。

### 5.2 治理闭环

1. 用户对违规内容发起举报。
2. 版主/管理员在后台处理举报。
3. 对违规内容执行下架或删除，必要时封禁用户。
4. 处理结果留档可审计。

---

## 6. 功能需求（按模块）

## 6.1 账号与认证

### 功能点

1. 邮箱/手机号注册（至少支持一种，默认邮箱）。
2. 账号密码登录、退出登录。
3. 个人信息修改（昵称、头像、简介）。
4. Token 续期与登录态管理。

### 业务规则

1. 昵称全站唯一，长度限制 2~24。
2. 密码强度最小规则（8 位，包含字母与数字）。
3. 被封禁用户不可登录或进行互动操作。

---

## 6.2 板块与帖子

### 功能点

1. 板块列表展示（名称、描述、帖子数）。
2. 帖子列表（最新、最热排序）。
3. 发帖（标题、正文、图片附件）。
4. 编辑/删除帖子（仅作者或管理员）。
5. 帖子详情（内容、作者、发布时间、互动数据）。

### 业务规则

1. 标题 5~100 字，正文上限由系统配置。
2. 帖子删除采用软删除。
3. 已下架帖子普通用户不可见。

---

## 6.3 评论与回复

### 功能点

1. 评论发布、回复评论（支持一层回复，预留楼中楼扩展）。
2. 评论分页加载。
3. 评论删除（作者、管理员）。

### 业务规则

1. 评论内容最小长度 2 字。
2. 评论删除后保留“该评论已删除”占位（可配置）。

---

## 6.4 互动与通知

### 功能点

1. 点赞/取消点赞。
2. 收藏/取消收藏。
3. 站内通知列表：被点赞、被评论、被回复。

### 业务规则

1. 点赞与收藏均需去重（同用户同帖子唯一）。
2. 通知默认未读，支持已读状态管理。

---

## 6.5 举报与审核

### 功能点

1. 用户可对帖子/评论举报并选择原因。
2. 管理后台查看举报列表、处理举报。
3. 审核动作：忽略、下架内容、删除内容、封禁用户。

### 业务规则

1. 举报需填写原因分类，可选补充说明。
2. 所有审核动作写入审计日志。

---

## 7. 上传与媒体（MinIO）

1. 头像、帖子图片、附件统一走 MinIO。
2. 采用“后端签发预签名 URL + 客户端直传”。
3. MinIO bucket 默认私有，访问通过时效 URL。
4. 上传需校验大小、类型、后缀白名单。

---

## 8. 响应式与体验要求（手机端 + PC）

1. Mobile First 设计；关键流程在 375px 宽度可完整操作。
2. 断点建议：
   - 手机：`<768`
   - 平板：`768~1023`
   - PC：`>=1024`
3. 核心页面（首页、列表、详情、发帖、登录）必须双端验证通过。
4. 交互可用性：可点击区域高度 >= 44px。

---

## 9. 非功能需求（NFR）

1. 可用性目标：月度可用性 >= 99.9%。
2. 性能目标（MVP）：
   - 帖子列表接口 P95 <= 300ms（缓存命中情况下）
   - 帖子详情接口 P95 <= 400ms
3. 安全目标：
   - 接口鉴权、限流、输入校验覆盖核心写操作
   - 关键管理操作具备审计能力
4. 数据可靠性：
   - 每日备份 PostgreSQL
   - MinIO 文件具备冗余策略

---

## 10. 数据埋点与指标口径

### 10.1 关键事件

1. `view_post`：查看帖子详情
2. `create_post`：发布帖子
3. `create_comment`：发表评论
4. `like_post`：点赞帖子
5. `favorite_post`：收藏帖子
6. `submit_report`：提交举报

### 10.2 核心指标

1. 内容生产率：`发帖用户数 / DAU`
2. 互动率：`有互动帖子数 / 总帖子数`
3. 举报率：`被举报帖子数 / 总帖子数`
4. 审核及时率：`24h 内处理举报数 / 举报总数`

---

## 11. 验收标准（Given / When / Then）

## 11.1 注册登录

1. Given 用户未登录，When 提交合法注册信息，Then 注册成功并可登录。
2. Given 用户输入错误密码，When 登录，Then 返回统一错误码且不泄露具体信息。

## 11.2 发帖

1. Given 用户已登录，When 填写标题与正文并提交，Then 帖子创建成功并可在列表看到。
2. Given 用户被封禁，When 尝试发帖，Then 返回无权限错误。

## 11.3 评论与通知

1. Given 用户 A 的帖子存在，When 用户 B 评论，Then 用户 A 通知列表出现“被评论”通知。
2. Given 用户 B 删除评论，When 用户 A 刷新详情，Then 评论展示按规则更新。

## 11.4 举报审核

1. Given 普通用户提交举报，When 版主处理为“下架”，Then 帖子对普通用户不可见。
2. Given 管理员执行封禁，When 被封禁用户再次登录，Then 登录失败并提示账号状态异常。

---

## 12. 发布准入（Go-Live Checklist）

1. P0 功能测试通过率 100%。
2. 核心接口压测达标（列表、详情、发帖、评论）。
3. 监控告警就绪（应用、数据库、Redis、MinIO）。
4. 灰度发布与回滚流程在 staging 演练通过。
5. 安全检查通过（鉴权、权限、输入校验、限流）。

---

## 13. 里程碑（按阶段，不按日历）

### M1：需求冻结

- 完成 PRD 评审并冻结 `v1.0` 范围。

### M2：技术冻结

- OpenAPI、ERD、RBAC、MinIO 上传方案评审通过。

### M3：可发布

- 功能联调、测试、压测、运维演练完成，达到发布准入。

---

## 14. 风险与应对

1. 风险：需求边界扩张导致延期。  
   应对：坚持 MVP 变更流程，新增需求进入下一版本池。
2. 风险：前后端接口变更频繁。  
   应对：以 OpenAPI 为唯一契约源，变更必须走评审。
3. 风险：审核压力高。  
   应对：先规则化审核 + 人工兜底，逐步引入自动化能力。
4. 风险：大图上传影响体验。  
   应对：MinIO 直传 + 前端压缩 + 附件大小限制。

---

## 15. 待确认事项（评审会现场确认）

1. 注册方式是否同时支持邮箱与手机号。
2. 是否允许游客查看全部板块内容。
3. 评论删除后是否显示“已删除”占位。
4. 举报分类枚举是否需要行业定制。
5. 上线阶段是否启用邀请制或白名单。

