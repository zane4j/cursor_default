# 论坛项目编码前文档清单（防返工版）

## 1. 使用方式（先看）

本清单用于在正式编码前做“设计冻结（Design Freeze）”，目标是减少以下返工：

- 需求理解偏差导致的重做
- 接口协议变更导致的前后端反复联调
- 数据模型设计不稳导致的迁移成本
- 权限与审核规则遗漏导致的上线风险

建议在进入开发前召开一次 60~90 分钟评审会，按本文档逐项过清单，并输出“可开工结论”。

---

## 2. 开工前必须具备的文档（MVP）

以下 12 份文档是建议的最小集合（MVP），每一项都给出目标、核心内容与完成标准。

### 2.1 PRD（产品需求文档）

**目标**：统一业务目标与范围，避免“做对了但不是要的”。

**核心内容**：

1. 用户画像与核心场景
2. MVP 范围（包含/不包含）
3. 关键流程（注册、发帖、评论、举报、审核）
4. 验收标准（按功能点可验证）

**完成标准（DoD）**：

- 每个功能点都有明确输入、输出、约束
- 每个页面/接口能映射到一个用户故事
- 明确列出“不做项（Out of Scope）”

---

### 2.2 用户故事与验收标准（User Stories + AC）

**目标**：把 PRD 变成可测试的开发单位。

**核心内容**：

1. 角色（普通用户、版主、管理员）
2. 用户故事（As a / I want / So that）
3. 验收标准（Given / When / Then）
4. 异常流（无权限、被封禁、内容违规）

**完成标准（DoD）**：

- 每条故事都能直接转化为测试用例
- 每条故事明确优先级（P0/P1/P2）

---

### 2.3 信息架构与页面清单（IA + Sitemap）

**目标**：避免前端页面结构反复调整。

**核心内容**：

1. 页面树（移动端/PC 共享）
2. 页面路由与导航关系
3. 每个页面的数据依赖与交互动作

**完成标准（DoD）**：

- 全部页面有唯一标识和入口路径
- 每个页面能对应到具体接口或占位接口

---

### 2.4 交互与 UI 规范（响应式）

**目标**：减少“开发完才发现移动端体验不合格”。

**核心内容**：

1. 核心页面低保真/高保真稿
2. 响应式断点策略（手机/平板/PC）
3. 通用组件规范（按钮、输入框、卡片、分页）
4. 空态、错误态、加载态

**完成标准（DoD）**：

- 关键流程页面（列表、详情、发帖、登录）均有设计稿
- 每个组件包含交互状态定义（默认/禁用/加载/错误）

---

### 2.5 接口契约文档（OpenAPI）

**目标**：前后端并行开发的“唯一真相源”。

**核心内容**：

1. API 路径、方法、版本（`/api/v1`）
2. 请求/响应 DTO
3. 错误码与错误语义
4. 鉴权要求（匿名/登录/角色）
5. 分页与排序约定

**完成标准（DoD）**：

- 覆盖 MVP 全部接口
- 可由文档生成 mock server 或客户端 SDK
- 至少完成一次前后端联合评审

---

### 2.6 数据模型文档（ERD + DDL 草案）

**目标**：防止后期频繁改表、迁移困难。

**核心内容**：

1. ER 图（核心实体关系）
2. 表结构草案（字段、类型、约束、索引）
3. 软删除、审计字段规范
4. 迁移策略（Flyway/Liquibase）

**完成标准（DoD）**：

- 高频查询场景都能映射到索引方案
- 关键约束已定义（唯一、外键、非空）
- 完成一次 SQL 评审（开发 + DBA/资深后端）

---

### 2.7 权限模型文档（RBAC）

**目标**：避免后期权限补丁式修复。

**核心内容**：

1. 角色定义：游客、用户、版主、管理员
2. 资源定义：帖子、评论、举报、管理接口
3. 操作矩阵：谁能做什么
4. 特殊规则：封禁、隐藏、申诉

**完成标准（DoD）**：

- 形成“角色-资源-动作”矩阵表
- 每个受限接口都可映射到权限规则

---

### 2.8 内容审核与风控策略文档

**目标**：避免上线后因违规内容或滥用导致运营风险。

**核心内容**：

1. 举报分类与处理流程
2. 审核状态机（待审/通过/驳回/下架）
3. 封禁策略（时长、范围、复审）
4. 防刷策略（频率限制、黑名单、设备/IP 维度）

**完成标准（DoD）**：

- 有明确 SLA（例如 24h 内处理举报）
- 审核动作均可追溯到审计日志

---

### 2.9 文件存储方案文档（MinIO 专项）

**目标**：避免上传链路和权限设计反复返工。

**核心内容**：

1. MinIO Bucket 规划与路径命名规范
2. 上传流程（预签名直传）
3. 文件访问控制（私有桶 + 临时下载 URL）
4. 文件校验（MIME/大小/后缀/hash）
5. 清理策略（孤儿文件回收）

**完成标准（DoD）**：

- 明确每类文件的生命周期与访问策略
- 前后端都能基于同一流程实现上传/回调

---

### 2.10 非功能需求文档（NFR）

**目标**：提前锁定性能与稳定性标准。

**核心内容**：

1. 性能目标（QPS、延迟、并发）
2. 可用性目标（SLA/SLO）
3. 容量规划（用户量、帖子增长、存储增长）
4. 灾备目标（RPO/RTO）

**完成标准（DoD）**：

- 每个核心接口有可量化性能指标
- 有可执行的容量预估表

---

### 2.11 测试策略文档

**目标**：避免“开发完成但不可验收”。

**核心内容**：

1. 测试分层：单元/集成/API/E2E
2. 冒烟用例（登录、发帖、评论、举报）
3. 回归清单
4. 测试数据与环境约定

**完成标准（DoD）**：

- 每个 P0 用户故事至少有 1 条自动化或可执行手工用例
- 发布前有固定冒烟脚本

---

### 2.12 部署与发布文档（Dev/Staging/Prod）

**目标**：避免“代码写完但上线不稳定”。

**核心内容**：

1. 环境拓扑（Nginx、Spring Boot、PostgreSQL、Redis、MinIO）
2. 配置管理（环境变量、密钥管理）
3. 发布策略（滚动/蓝绿）
4. 回滚策略与故障处置流程

**完成标准（DoD）**：

- 在 staging 完成一键部署演练
- 回滚步骤可在 15 分钟内执行完成

---

## 3. 推荐补充文档（项目增长后）

1. 搜索方案设计（ES/Meilisearch）
2. 消息队列与异步任务设计
3. 数据归档与冷热分层策略
4. 可观测性看板与告警规则明细
5. 法务与合规流程文档（隐私、数据导出、注销）

---

## 4. 文档之间的依赖关系（先后顺序）

建议顺序：

1. PRD
2. 用户故事 + 验收标准
3. IA + 交互/UI
4. 接口契约（OpenAPI）
5. 数据模型（ERD + DDL）
6. 权限模型 + 审核风控
7. MinIO 存储专项
8. NFR + 测试策略
9. 部署与发布文档

说明：

- 第 4、5 步必须同步评审，避免 DTO 与表结构错位
- 第 6、7 步会影响接口字段和状态机，不建议后置

---

## 5. 评审机制（防返工关键）

建议设置 3 个 Gate：

### Gate A：需求冻结

- PRD、用户故事、页面清单通过
- 输出版本号（如 `v1.0-requirements-frozen`）

### Gate B：技术冻结

- OpenAPI、ERD、权限模型、MinIO 方案通过
- 输出版本号（如 `v1.0-tech-frozen`）

### Gate C：可发布确认

- 测试策略、部署文档、回滚预案通过
- 输出版本号（如 `v1.0-release-ready`）

---

## 6. 快速开工检查表（可直接勾选）

- [ ] PRD 已明确 MVP 边界和不做项
- [ ] 用户故事均有验收标准（Given/When/Then）
- [ ] 页面清单覆盖移动端与 PC 关键流程
- [ ] OpenAPI 覆盖全部 MVP 接口并完成联评
- [ ] ERD/DDL 与接口字段已对齐
- [ ] RBAC 权限矩阵已评审
- [ ] 举报/审核/封禁状态机已确定
- [ ] MinIO 上传与访问策略已确定
- [ ] NFR 指标有量化目标
- [ ] 测试计划包含冒烟与回归
- [ ] 部署/回滚流程已在 staging 演练

当以上项目全部勾选后再进入大规模编码，能显著降低返工概率。

