# 论坛系统 NFR / SLO / SLA 规范

## 1. 文档信息

- 文档版本：`v1.0-draft`
- 适用范围：论坛 MVP（前台 + 管理后台）
- 关联文档：
  - `PRD.md`
  - `ARCHITECTURE.md`
  - `OPENAPI_CONTRACT.md`
  - `ERD_DDL.md`
  - `MODERATION_WORKFLOW.md`

---

## 2. 目标与适用边界

## 2.1 目标

1. 在编码前冻结可量化非功能指标，避免“功能完成但不可上线”。
2. 为研发、测试、运维提供统一验收口径。
3. 为后续容量扩展与故障恢复建立可执行基线。

## 2.2 边界

本规范覆盖：

1. 性能（延迟、吞吐、并发）
2. 可用性与可靠性（SLO/SLA）
3. 容量与增长预估
4. 备份、容灾与恢复目标

本期不覆盖：

1. 多地域多活架构
2. 跨云容灾编排
3. 超大规模（亿级 DAU）容量模型

---

## 3. 术语与口径

1. **NFR**：非功能需求，定义系统质量目标。
2. **SLI**：服务级指标（可观测数据，如可用率、P95 时延）。
3. **SLO**：服务级目标（团队内部承诺目标）。
4. **SLA**：服务级协议（对外承诺门槛）。
5. **RTO**：故障恢复时间目标（可接受最长恢复时长）。
6. **RPO**：故障恢复点目标（可接受最大数据丢失时间窗）。

统计口径默认：

1. 时间窗口：按自然月统计（SLA），按周统计（SLO）。
2. 区域：生产环境全量请求。
3. 延迟：服务端耗时（不含客户端渲染）。

---

## 4. 可用性与可靠性目标（SLI/SLO/SLA）

## 4.1 核心 SLI

1. `availability`：成功请求数 / 总请求数（5xx 与超时计失败）。
2. `api_latency_p95`：核心 API P95。
3. `api_latency_p99`：核心 API P99。
4. `error_rate`：5xx 占比。
5. `db_connection_utilization`：数据库连接池使用率。
6. `redis_hit_rate`：Redis 命中率（核心缓存）。
7. `report_handle_latency`：举报从创建到处理的耗时。

## 4.2 SLO（内部目标）

| 指标 | SLO 目标 | 统计窗口 |
| --- | --- | --- |
| 平台可用率 | `>= 99.95%` | 周/月 |
| 核心读 API（列表/详情）P95 | `<= 300ms` | 5 分钟滚动 |
| 核心写 API（发帖/评论）P95 | `<= 500ms` | 5 分钟滚动 |
| 核心 API 错误率 | `<= 0.5%` | 5 分钟滚动 |
| 举报处理及时率（24h） | `>= 95%` | 日/周 |
| MinIO 上传成功率 | `>= 99.9%` | 日/周 |

## 4.3 SLA（对外承诺，MVP）

| 指标 | SLA 承诺 |
| --- | --- |
| 月度可用率 | `>= 99.9%` |
| 工单/故障响应（P1） | `<= 30 分钟` |
| 举报处理（普通） | `<= 24 小时` |
| 举报处理（高危） | `<= 2 小时`（需 reasonCode 标记） |

说明：

1. 若当月可用率低于 SLA，进入故障复盘并执行改进计划。
2. SLA 豁免需明确记录（计划维护窗口、第三方依赖重大故障）。

---

## 5. 性能目标（API 级）

## 5.1 核心接口目标（生产）

| 接口 | 目标 QPS（MVP） | P95 | P99 | 错误率 |
| --- | --- | --- | --- | --- |
| `GET /api/v1/posts` | 200 | <= 300ms | <= 600ms | <= 0.5% |
| `GET /api/v1/posts/{id}` | 250 | <= 300ms | <= 650ms | <= 0.5% |
| `GET /api/v1/posts/{id}/comments` | 150 | <= 350ms | <= 700ms | <= 0.5% |
| `POST /api/v1/posts` | 30 | <= 500ms | <= 900ms | <= 1.0% |
| `POST /api/v1/posts/{id}/comments` | 60 | <= 500ms | <= 900ms | <= 1.0% |
| `POST /api/v1/auth/login` | 50 | <= 400ms | <= 800ms | <= 1.0% |
| `POST /api/v1/reports` | 20 | <= 450ms | <= 850ms | <= 1.0% |

## 5.2 前端体验目标（用户感知）

1. 首屏可交互时间（TTI）：
   - 移动端 4G：`<= 3.5s`
   - PC 宽带：`<= 2.5s`
2. 关键操作反馈：
   - 点击按钮到 loading 反馈：`<= 100ms`
   - 成功/失败提示出现：`<= 300ms`

## 5.3 资源消耗阈值（forum-api）

1. CPU 平均使用率：`< 70%`，峰值 `< 85%`。
2. JVM 堆使用率：常态 `< 75%`，Full GC 频率可控。
3. 线程池排队长度：长期不超过阈值（需按线程池类型定义）。

---

## 6. 容量规划与增长假设

## 6.1 MVP 假设（初始容量）

1. 注册用户总量：`10 万`
2. DAU：`1 万`
3. 峰值并发在线：`1500`
4. 日发帖：`3000`
5. 日评论：`15000`
6. 日上传文件：`5000`（头像 + 帖子图）

## 6.2 数据增长估算（按日）

1. `posts`：约 `3k/day`，按平均 2KB 文本估算 `~6MB/day`（不含索引）。
2. `comments`：约 `15k/day`，按平均 0.6KB 文本估算 `~9MB/day`。
3. `notifications`：约 `30k/day`，按平均 0.4KB 估算 `~12MB/day`。
4. MinIO 对象：按平均 300KB、`5k/day` 估算 `~1.5GB/day`。

## 6.3 预警阈值（建议）

| 资源 | 预警阈值 | 严重阈值 |
| --- | --- | --- |
| PostgreSQL 存储使用率 | 70% | 85% |
| MinIO 存储使用率 | 70% | 85% |
| Redis 内存使用率 | 75% | 90% |
| DB 连接池使用率 | 70% | 90% |
| API 实例 CPU | 75% | 90% |

## 6.4 扩容触发策略

1. 连续 3 天触发“预警阈值” -> 提交扩容工单。
2. 任一指标达到“严重阈值” -> 当日执行扩容或流量保护策略。
3. 每月至少一次容量复盘，更新下一月预测。

---

## 7. 备份与容灾目标（RPO/RTO）

## 7.1 PostgreSQL

1. 备份策略：
   - 每日全量备份（低峰时段）
   - 每 15 分钟增量/WAL 归档（推荐）
2. 保留策略：
   - 全量备份保留 14~30 天（按成本策略）
3. 恢复目标：
   - `RPO <= 15 分钟`
   - `RTO <= 60 分钟`

## 7.2 MinIO

1. 备份/冗余策略：
   - 建议启用纠删码（Erasure Coding）或跨盘冗余
   - 定期对象校验与生命周期策略
2. 恢复目标：
   - `RPO <= 60 分钟`（依赖同步策略）
   - `RTO <= 120 分钟`

## 7.3 Redis

1. 持久化策略：
   - `AOF everysec`（推荐）或 `RDB + AOF`
2. 数据恢复定位：
   - Redis 作为缓存/会话，允许可控级别数据重建
3. 恢复目标：
   - `RPO <= 5 分钟`（有 AOF）
   - `RTO <= 30 分钟`

## 7.4 恢复演练要求

1. 每季度至少 1 次“备份恢复演练”。
2. 演练必须记录：
   - 实际 RTO/RPO
   - 问题清单与改进项
3. 演练结果纳入发布准入审查。

---

## 8. 监控、告警与值班

## 8.1 监控面板最小集合

1. API：QPS、P95/P99、错误率、超时率。
2. JVM：堆内存、GC、线程池、类加载。
3. 数据库：连接池、慢查询、锁等待、事务回滚率。
4. Redis：命中率、内存、淘汰数、连接数。
5. MinIO：请求成功率、4xx/5xx、吞吐、容量增长。
6. 业务：待处理举报量、处理 SLA 达成率。

## 8.2 告警规则建议

1. `availability < 99.9%`（5 分钟窗口） -> P1 告警。
2. `api_latency_p95` 连续 10 分钟超标 -> P2 告警。
3. `error_rate > 2%` 持续 5 分钟 -> P1 告警。
4. `report_pending_count` 超阈值（如 > 500）-> 运营告警。
5. 存储/内存达到严重阈值 -> 平台告警。

## 8.3 值班要求（MVP）

1. 至少建立工作时间值班机制。
2. P1 故障 30 分钟内响应，2 小时内给出临时止损方案。
3. 所有 P1/P2 需输出复盘文档。

---

## 9. 发布准入（NFR Gate）

发布前必须满足：

1. 核心接口压测结果达到第 5 章目标。
2. 可用性、错误率、时延面板可实时观测。
3. 备份任务和恢复脚本已验证可执行。
4. 至少一次完整预发布故障演练（数据库恢复或服务回滚）。
5. 安全基线通过（鉴权、限流、输入校验、审计）。

不满足任一项，默认禁止上线。

---

## 10. 角色职责（RACI 简版）

| 事项 | 产品 | 后端 | 前端 | 测试 | 运维 |
| --- | --- | --- | --- | --- | --- |
| NFR 目标定义 | A | R | C | C | C |
| SLI/SLO 监控落地 | C | R | C | C | A |
| 压测执行与报告 | C | R | C | A | C |
| 备份恢复演练 | C | C | N/A | C | A/R |
| 故障复盘 | C | R | C | C | A |

说明：

- `R`：负责执行
- `A`：最终负责
- `C`：协作/评审

---

## 11. 待确认事项

1. 是否要求 7*24 值班（当前按 MVP 工作时间值班）。
2. 是否在 MVP 阶段引入异地灾备（当前默认不启用）。
3. MinIO 备份是同城双副本还是跨地域副本。
4. 是否对管理后台单独定义更严格 SLA。
5. 是否引入更细颗粒度 SLO（按端点分层承诺）。

