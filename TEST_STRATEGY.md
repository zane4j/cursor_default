# 论坛系统测试策略（MVP）

## 1. 文档信息

- 文档版本：`v1.0-draft`
- 适用范围：论坛 MVP（后端 Spring Boot 3 + 前端 Web）
- 关联文档：
  - `PRD.md`
  - `OPENAPI_CONTRACT.md`
  - `ERD_DDL.md`
  - `INTERACTION_UI_SPEC.md`
  - `NFR_SLO_SLA.md`

---

## 2. 测试目标与范围

## 2.1 目标

1. 在编码与发布前尽早发现功能、接口、权限、性能、稳定性问题。
2. 保障核心业务链路可用：登录 -> 浏览 -> 发帖 -> 评论 -> 举报 -> 审核。
3. 用自动化测试覆盖高频回归场景，降低手工回归成本。

## 2.2 范围

覆盖：

1. 后端：业务逻辑、接口契约、权限与状态机、数据一致性。
2. 前端：关键页面渲染、交互流程、响应式适配（手机端/PC）。
3. 端到端：核心用户旅程与治理旅程。

暂不覆盖（MVP）：

1. 全量跨浏览器组合（先覆盖 Chrome 主流版本）。
2. 全量安全渗透测试（先做基础安全扫描与策略校验）。

---

## 3. 测试分层策略

## 3.1 测试金字塔目标占比

建议占比（以用例数量计）：

1. 单元测试（Unit）：`60%`
2. 集成测试（Integration）：`25%`
3. API 合约测试（Contract）：`10%`
4. 端到端测试（E2E）：`5%`

说明：

- E2E 成本高，聚焦 P0 业务链路。
- 权限、状态机、事务一致性必须至少有集成测试覆盖。

## 3.2 测试类型定义

1. **Unit**：单类/单函数，隔离外部依赖，验证业务规则。
2. **Integration**：服务层 + 数据库/缓存/对象存储（测试替身或测试实例）。
3. **Contract**：按 OpenAPI 约束验证请求/响应结构与错误码。
4. **E2E**：从 UI 或 API 入口走完整业务流程。
5. **Smoke**：发布后快速确认核心功能可用。

---

## 4. 测试环境与数据策略

## 4.1 环境分层

1. `local`：开发者本地快速验证（单元 + 局部集成）。
2. `ci`：持续集成环境（自动化主战场）。
3. `staging`：预发布环境（接近生产配置）。
4. `prod`：生产环境（仅 smoke + 监控验证）。

## 4.2 依赖组件策略

1. PostgreSQL：集成测试优先使用临时实例（如 Testcontainers）。
2. Redis：集成测试使用独立实例，测试前后清理命名空间。
3. MinIO：测试桶隔离（如 `forum-test-media`），按用例回收对象。

## 4.3 测试数据原则

1. 测试数据可重复构建，不依赖人工状态。
2. 用例间数据隔离，避免互相污染。
3. 每条 E2E 用例使用独立测试账号或前缀。
4. 覆盖边界数据：超长标题、非法 MIME、重复点赞、并发审核。

---

## 5. 单元测试策略（Unit）

## 5.1 覆盖对象

1. 领域规则：
   - 帖子标题长度校验
   - 评论最小长度校验
   - 举报目标合法性校验
2. 权限判断函数：
   - 是否作者
   - 版主板块范围校验
3. 状态机逻辑：
   - 举报状态迁移合法性
   - 审核动作映射
4. DTO 校验器与转换器。

## 5.2 覆盖指标

最低门槛（MVP）：

1. 后端核心模块行覆盖率 >= `70%`
2. 关键模块（auth/post/comment/moderation）行覆盖率 >= `80%`
3. 分支覆盖率建议 >= `60%`

---

## 6. 集成测试策略（Integration）

## 6.1 重点场景

1. 帖子创建事务（含附件引用记录）。
2. 评论创建后帖子评论计数更新。
3. 点赞/收藏幂等（唯一约束 + 业务返回码）。
4. 举报处理事务闭环：
   - 更新举报状态
   - 更新目标状态
   -（可选）更新用户状态
   - 写审计日志
5. MinIO 上传确认链路（签发凭证 -> 确认记录）。

## 6.2 一致性校验

1. 任一步骤失败应整体回滚。
2. 并发处理同一举报单，只有一个请求成功。
3. 重复提交幂等键返回一致结果。

---

## 7. API 合约测试（Contract）

## 7.1 目标

确保服务实现不偏离 `OPENAPI_CONTRACT.md`。

## 7.2 校验维度

1. 路径与方法是否存在。
2. 请求体字段类型与必填规则。
3. 响应结构统一（`code/message/data/requestId`）。
4. 错误码语义一致（如 `40301`、`40901`）。
5. 分页字段完整（`list/page/pageSize/total`）。

## 7.3 关键接口清单（P0）

1. `POST /auth/register`
2. `POST /auth/login`
3. `GET /posts`
4. `POST /posts`
5. `POST /posts/{id}/comments`
6. `POST /reports`
7. `PATCH /moderation/reports/{id}/resolve`
8. `POST /media/upload-ticket`
9. `POST /media/confirm`

---

## 8. E2E 测试策略（端到端）

## 8.1 核心链路（P0）

### E2E-01 用户内容闭环

1. 用户注册并登录。
2. 创建帖子并上传图片。
3. 另一用户评论并点赞。
4. 作者收到通知并可查看详情。

### E2E-02 举报审核闭环

1. 用户提交帖子举报。
2. 版主处理为下架。
3. 普通用户确认内容不可见。
4. 审核日志可检索到该动作。

### E2E-03 权限防护

1. 未登录用户尝试发帖/评论 -> 应被拒绝。
2. 普通用户调用审核接口 -> `403`。
3. 非作者编辑帖子 -> `403`。

### E2E-04 媒体上传闭环

1. 获取 MinIO 上传凭证。
2. 直传对象并确认。
3. 获取下载链接并访问成功。

## 8.2 响应式验证（手工 + 自动）

最小手工清单：

1. 375 宽度下：首页、列表、详情、发帖、登录可完整操作。
2. 1024+ 宽度下：双栏布局正常，按钮 hover/focus 正常。
3. 触控目标尺寸符合规范（>=44px）。

---

## 9. 冒烟测试清单（Smoke）

发布后 10 分钟内完成以下检查：

1. 登录成功（普通用户）。
2. 帖子列表和详情可访问。
3. 发帖成功并可见。
4. 评论成功并可见。
5. 点赞/收藏可用。
6. 举报提交成功。
7. 版主可处理举报。
8. MinIO 上传与下载链接可用。
9. 核心监控无红色告警（应用/DB/Redis/MinIO）。

---

## 10. 非功能测试策略

## 10.1 性能测试

基于 `NFR_SLO_SLA.md` 指标执行：

1. 列表接口：`P95 <= 300ms`
2. 详情接口：`P95 <= 400ms`
3. 登录接口：`P95 <= 350ms`

压测策略：

1. 先单接口压测，再混合流量压测。
2. 使用固定数据规模 + 热点帖子场景。
3. 输出报告：吞吐、延迟、错误率、资源占用。

## 10.2 稳定性测试

1. 长稳压测（建议 2 小时）观察内存与连接泄漏。
2. 异常注入：
   - Redis 短时不可用
   - MinIO 超时
   - 数据库慢查询

## 10.3 备份恢复演练

1. PostgreSQL 备份恢复可用性验证。
2. MinIO 样本对象恢复验证。
3. 记录 RTO/RPO 实测值并与目标对比。

---

## 11. 缺陷分级与准入标准

## 11.1 缺陷等级

1. `P0`：核心链路不可用/数据错误/安全风险。
2. `P1`：主要功能受损，有替代路径。
3. `P2`：体验问题或低频功能问题。
4. `P3`：优化项。

## 11.2 发布准入

满足以下条件方可发布：

1. `P0` 缺陷 = `0`
2. `P1` 缺陷已修复或有明确豁免审批
3. 自动化测试通过率 >= `98%`
4. P0 冒烟用例通过率 = `100%`
5. 性能指标达标（按 NFR）

---

## 12. CI/CD 测试门禁建议

## 12.1 Pull Request 阶段

1. 单元测试必跑。
2. 合约测试必跑。
3. 关键集成测试（auth/post/moderation）必跑。
4. 静态检查（lint/format/spotbugs/checkstyle）必跑。

## 12.2 合并主干阶段

1. 全量集成测试。
2. 关键 E2E（P0）用例。
3. 产出测试报告并归档。

## 12.3 发布阶段（staging -> prod）

1. staging 冒烟通过后才允许发布。
2. 发布后自动触发生产 smoke。
3. smoke 失败自动告警并触发回滚预案。

---

## 13. 测试用例清单（可直接建任务）

## 13.1 账号与权限

- [ ] 注册成功/失败边界
- [ ] 登录成功/错误密码/封禁账号
- [ ] 非法 token/过期 token/刷新 token
- [ ] 未授权访问受限接口返回 `401/403`

## 13.2 内容与互动

- [ ] 发帖字段校验与成功流
- [ ] 编辑/删除帖子权限校验
- [ ] 评论创建/删除与计数更新
- [ ] 重复点赞/收藏幂等与错误码校验

## 13.3 治理与状态机

- [ ] 举报创建成功与限流
- [ ] 审核动作 `IGNORE/HIDE/DELETE/BAN` 映射正确
- [ ] 并发处理同一举报单冲突控制
- [ ] 审计日志字段完整性

## 13.4 媒体链路

- [ ] 上传凭证签发校验（MIME/大小）
- [ ] MinIO 直传后确认登记成功
- [ ] 下载 URL 时效与权限校验

## 13.5 响应式与可用性

- [ ] 手机端关键流程可达
- [ ] PC 端关键布局与交互状态
- [ ] 表单错误文案一致性
- [ ] A11y 基线（focus、aria-label、对比度）

---

## 14. 角色分工建议

1. 开发：单元测试、部分集成测试、缺陷自测。
2. QA：集成、E2E、冒烟与回归。
3. 运维/SRE：发布后 smoke、监控验证、备份恢复演练。
4. 产品/运营：验收业务流程与审核策略符合预期。

---

## 15. 待确认事项

1. E2E 工具选型（Playwright/Cypress）最终确定。
2. 是否在 MVP 阶段引入 API Mock Server 作为前后端并行手段。
3. 生产 smoke 是否全部自动化，或保留人工复核步骤。
4. 稳定性压测时长是否提升至 4 小时。
5. 可访问性是否在 MVP 就纳入强制门禁。

